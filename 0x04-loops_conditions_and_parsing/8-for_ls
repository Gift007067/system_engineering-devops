#!/usr/bin/env bash
# Lists files in current directory, printing part after first dash, or full name if no dash

for file in *; do
  [[ "$file" == .* ]] && continue
  if [[ "$file" == *-* ]]; then
    echo "${file#*-}"
  else
    echo "$file"
  fi
done
